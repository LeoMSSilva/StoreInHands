import React from 'react';
import { Input } from './style';

export function InputText({ placeholder }) {
	return <Input placeholder={placeholder} />;
}

export function InputPassorwd({ placeholder }) {
	return <Input placeholder={placeholder} />;
}
